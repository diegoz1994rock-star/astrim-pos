# Reglas mínimas — minifyEnabled queda en false por ahora (ver
# build.gradle.kts, buildTypes.release). Revisar cuando se active minify de
# verdad, ya con la capa de red/serialización que esta primera fase todavía
# no tiene.
