<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Sin permisos de red todavía a propósito: esta primera fase es solo
         la estructura de pantallas, sin sincronización ni servidor (ver
         PROGRESS.md del módulo). Se agrega INTERNET cuando exista de
         verdad algo que consultar. -->

    <!-- Solo para "Crear respaldo" en Android 9 (API 28) o anterior, donde
         escribir en Descargas todavía requiere este permiso clásico; desde
         Android 10 (API 29) se usa MediaStore y no hace falta (ver
         BackupManager.createBackupToDownloads). -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
        android:name=".LicenseManagerApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:theme="@style/Theme.AstrimLicenseManager">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.AstrimLicenseManager">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
