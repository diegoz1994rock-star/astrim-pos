# Reglas mínimas — minifyEnabled queda en false por ahora (ver
# build.gradle.kts, buildTypes.release). Se revisan en la Fase 8
# (Optimización y preparación para producción) cuando se active minify de
# verdad; documentar acá las reglas de Gson/Retrofit que hagan falta en ese
# momento (mantener los nombres de campo de los DTOs, típicamente).
