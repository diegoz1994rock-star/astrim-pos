# PROGRESS.md

Estado vivo del proyecto. **Leer este archivo al comienzo de cualquier sesión de trabajo sobre este proyecto antes de continuar.** Se actualiza cada vez que se completa una unidad de trabajo relevante o antes de que el contexto de la sesión se agote.

## Modo de trabajo (confirmado con el usuario, 2026-07-09)

- Autonomía total: no se pausa a pedir aprobación entre fases ni entre módulos (esto reemplaza explícitamente las instrucciones de "esperar aprobación" de PROJECT_SPEC.md).
- Si se detecta una decisión de arquitectura mejor a mitad de camino, se explica brevemente en este archivo o en ARCHITECTURE.md y se aplica si no rompe lo ya construido.
- Al acercarse al límite de contexto: guardar aquí el punto exacto de avance y continuar automáticamente en la siguiente sesión, sin esperar instrucción del usuario.

## Estado por hito (ver ROADMAP.md)

| Hito | Estado |
|---|---|
| M0 — Documentos de fundación | ✅ completo |
| M1 — Estructura de carpetas | ✅ completo |
| M2 — Base de datos completa | ✅ completo |
| M3 — Núcleo y configuración inicial | ✅ completo |
| M4 — Identidad y acceso | ✅ completo |
| M5 — Catálogo e inventario | ✅ completo |
| M6 — Terceros | ✅ completo |
| M7 — Operación de venta | 🔄 en progreso (Ventas completo; Facturación/PDF, Promociones y Restaurante pendientes) |
| M8 — Operación de restaurante | pendiente |
| M9 — Reportes | 🔄 en progreso (ventas/productos/inventario/caja completos; ganancias/pérdidas, clientes, usuarios, impuestos pendientes) |
| M10 — Comercialización | 🔄 en progreso (Licencias completo; Backups pendiente) |
| M11 — Multi-estación | pendiente |
| M12 — Distribución | pendiente |
| M13 — App Android | fase posterior |

## Log de sesiones

### Sesión 1 — 2026-07-09

- Leído PROJECT_SPEC.md completo.
- Resuelta contradicción entre spec (pide aprobación por fase) y la instrucción del usuario (autonomía total): el usuario confirmó autonomía total explícitamente vía pregunta directa.
- Inicializado repositorio git local (sin remoto).
- Completado M0: creados ROADMAP.md, ARCHITECTURE.md, DATABASE.md, MODULES.md, FOLDER_STRUCTURE.md, TECHNOLOGIES.md, DEVELOPMENT_RULES.md.
- Decisiones de arquitectura clave fijadas (no reabrir sin razón fuerte, documentadas en ARCHITECTURE.md):
  - Monolito modular, Clean Architecture por módulo, capas domain/application/infrastructure/presentation.
  - Comunicación entre módulos vía bus de eventos interno, no imports cruzados.
  - SQLAlchemy + Alembic + SQLite por defecto, PK entera local + `uuid` global para sync.
  - Licencias: token firmado Ed25519 + anti-retroceso de reloj + verificación online oportunista (no depende solo del reloj del sistema).
  - Sincronización: estación servidor principal con FastAPI+WebSockets embebido, last-write-wins con log de conflictos.
  - Idioma: código en inglés, documentación y UI en español.
- Completado M1: árbol de carpetas físico completo (core, shared_ui, 21 módulos con capas domain/application/infrastructure/presentation, tests, migrations, scripts, docs, resources, installer, android), `pyproject.toml`, `.gitignore`, `README.md`, `alembic.ini`, `src/pos/main.py` mínimo (solo abre ventana vacía, sin lógica de negocio).
- Creado `.venv` con Python 3.13 (el Python 3.15 del sistema es una beta sin wheels de PySide6 disponibles aún — usar 3.13 para desarrollo hasta que el ecosistema se ponga al día). Instalación de dependencias (`pip install -e ".[dev]"`) lanzada en background; verificar resultado al retomar con `Read` sobre el output del proceso o reintentando `pip install -e ".[dev]"`.
- Instalación de dependencias verificada: `.venv` con Python 3.13 funcional, todas las librerías de TECHNOLOGIES.md importan correctamente, `python -m pos.main` abre una `QMainWindow` vacía sin errores.
- Completado M2: `core/database/base.py` (mixins de auditoría: `UUIDMixin`, `TimestampMixin`, `UserStampMixin`, `SoftDeleteMixin`), `core/database/session.py` (engine + `session_scope`), 54 modelos SQLAlchemy en los 20 módulos de negocio (ver DATABASE.md, sección "Implementación"), `core/database/model_registry.py` como registro central, `migrations/env.py` + `script.py.mako`, primera migración Alembic autogenerada y verificada, `scripts/seed_demo_data.py` verificado end-to-end. `ruff check` y `mypy --strict` pasan limpio sobre todo `src/`.
- Decisión de arquitectura fijada durante la implementación (documentada en ARCHITECTURE.md §12b y DATABASE.md): las FK entre módulos se declaran por nombre de tabla en string (`ForeignKey("products.id")`), nunca importando la clase ORM de otro módulo; los enums de estado se implementan como `enum.Enum` de Python + `sqlalchemy.Enum(native_enum=False)`, no como tablas de catálogo, salvo que el valor deba ser configurable desde la UI (roles, permisos, promociones sí son tablas).
- Completado M3: `core/exceptions` (jerarquía `DomainError`), `core/events` (`DomainEvent` + `EventBus` síncrono en memoria), `core/di` (`Container` con factory/singleton/instance), `core/logging` (consola + archivo rotativo), `core/config/bootstrap.py` (config de arranque en `~/.pos_system/config.toml`, distinta de la config de negocio en BD — ver docstring del módulo), `core/security` (`hash_password`/`verify_password` con Argon2, `SessionManager`/`ActiveSession`, decorador `require_permission`), `shared_ui/theme` (tokens claro/oscuro + `ThemeManager` que genera QSS), y el primer caso de uso real: `modules/settings/application/business_settings_service.py` (lectura con caché + escritura transaccional de `business_settings`, publica `BusinessSettingChangedEvent`). `main.py` ahora es una raíz de composición real: carga bootstrap, inicializa logging + engine, registra servicios en el DI container, aplica el tema por defecto. 19 pruebas (unitarias de `core` + integración de `settings`/`core` contra SQLite real) pasan; `ruff` y `mypy --strict` limpios.
- **Nota de entorno (no aplica al código del proyecto)**: en este sandbox de desarrollo, `pip` deja archivos con el atributo "oculto" del sistema de archivos (macOS `UF_HIDDEN`), lo que rompe la instalación editable (`import pos` falla) y la carga de plugins nativos de Qt (`offscreen` no se encuentra). Solución local: `chflags -R nohidden .venv` tras instalar dependencias. Para que las pruebas no dependan de esto se agregó `pythonpath = ["src"]` a `[tool.pytest.ini_options]` en `pyproject.toml` (solución robusta, sí forma parte del repo). Documentado también en README.md.
- Completado el módulo Login/Autenticación (`modules/auth`): `AuthenticationService` (login con bloqueo por intentos fallidos, sesión persistida, permisos efectivos vía join `users`→`roles`→`role_permissions`→`permissions`, eventos `LoginSucceededEvent`/`LoginFailedEvent`/`AccountLockedEvent`/`LogoutEvent`), `LoginView`/`LoginViewModel` (PySide6, MVVM), cableado como primera pantalla real en `main.py` (login → bienvenida con logout → login). 12 pruebas de integración + 3 pruebas `pytest-qt` nuevas, 32 pruebas totales pasando.
- **Bug real encontrado y corregido durante la verificación**: `AuthenticationService.login()` lanzaba la excepción de credenciales inválidas *dentro* del bloque `with session_scope()`, lo que causaba rollback y descartaba el `LoginAttempt` fallido recién registrado — el bloqueo por intentos fallidos nunca se activaba. Corregido guardando el error en una variable y lanzándolo después de que el bloque haga commit. Detectado por las pruebas de integración de `test_account_locks_after_max_failed_attempts`, no por inspección visual — confirma el valor de las pruebas de integración para casos de uso transaccionales (ver DEVELOPMENT_RULES.md, sección Testing, actualizada con esta distinción).
- **Bug visual real encontrado y corregido mediante verificación manual con capturas de pantalla**: la regla QSS global `QWidget { background-color: ... }` pintaba un fondo opaco en cada widget hijo (ej. el título dentro de la tarjeta de login), rompiendo el anidamiento visual. Corregido quitando `background-color` de la regla `QWidget` genérica y dejándolo solo en `QMainWindow`/`QDialog`, para que los contenedores hijos (QFrame#surface) sean los que definen su propio fondo y el resto quede transparente.
- Completado M4: módulo Usuarios (`modules/users`) — `UserManagementService` (crear/activar-desactivar/reset de contraseña, valida username único y longitud mínima de contraseña, eventos `UserCreatedEvent`/`UserStatusChangedEvent`) y módulo Roles (`modules/roles`) — `RoleManagementService` (crear rol personalizado, reemplazar permisos asignados, eliminar rol con protección contra roles del sistema y roles en uso). UI PySide6 completa: `UsersView` (tabla + alta + activar/desactivar) y `RolesView` (lista de roles + checklist de permisos), ambas cableadas en un panel "Administración" accesible desde la pantalla de bienvenida solo si la sesión tiene el permiso `users.manage`. 17 pruebas de integración nuevas (49 en total), todas pasando; `ruff`/`mypy --strict` limpios; verificado visualmente con capturas (tabla de usuarios con el admin semilla, matriz de permisos de "Administrador General" con sus 10 permisos correctos).
- **Dos bugs reales más encontrados y corregidos durante esta etapa** (además del de M4/Login): (1) `replace_role_permissions` agregaba filas `RolePermission` pero una lectura inmediatamente posterior en la misma transacción no las veía — causa raíz: la sesión global se creaba con `autoflush=False` sin buena justificación. Se cambió a `autoflush=True` (el default de SQLAlchemy) en `core/database/session.py`, eliminando toda una clase de bugs de "lectura después de escritura" en vez de parchear cada sitio con `flush()` manual. (2) SQLite no aplica restricciones de clave foránea por conexión a menos que se ejecute `PRAGMA foreign_keys=ON` explícitamente — nuestras 54 tablas declaraban FKs que **no se estaban validando en absoluto**. Corregido con un listener `event.listens_for(engine, "connect")` en `init_engine()`. Esto habilitó borrar la implementación cruzada `roles -> users` que iba a violar la dirección de dependencia documentada en MODULES.md: ahora `RoleRepository.delete_role` simplemente deja que la FK `ON DELETE RESTRICT` falle con `IntegrityError`, traducido a `BusinessRuleViolationError` en la capa de aplicación, sin que `roles` necesite importar `users`.
- Completado el módulo Productos y Categorías (`modules/products`): `CategoryManagementService` (jerárquico vía `parent_id`) y `ProductManagementService` (productos simples/compuestos/combo, asignación de impuestos, `add_recipe_item`/`add_combo_item` con validación de tipo de producto, eventos `ProductCreatedEvent`/`ProductStatusChangedEvent`). UI completa: `CategoriesView`, `ProductsView` + `ProductFormDialog` + `RecipeComboDialog` (composición de receta/combo), cableadas en un panel "Catálogo" gateado por el permiso `products.manage`. 15 pruebas de integración nuevas (64 en total); `ruff`/`mypy --strict` limpios; verificado visualmente (creación de producto vía servicio se refleja correctamente en la tabla con categoría resuelta).
- **Refactor de main.py**: se generalizó el patrón "botón en bienvenida → panel con permiso" que se repetía por cada módulo (`NavPanel` con `label`/`permission_code`/`on_click`, más `_wrap_with_back_button`/`open_panel` genéricos) en vez de escribir `build_admin_widget`, `build_catalog_widget`, etc. a mano por cada módulo nuevo — reduce el costo de cablear cada módulo restante a agregar una entrada a la lista `panels` en `show_welcome`.
- Completado M5 con el módulo Inventario (`modules/inventory`): `InventoryService` (entradas/salidas/ajustes con dirección aumentar-disminuir/transferencias entre bodegas, todas validando stock suficiente y publicando `StockLevelChangedEvent` con la bandera `is_below_minimum`). **Primer caso real de suscripción entre módulos**: `InventoryProductEventHandlers.on_product_created` se suscribe a `ProductCreatedEvent` (publicado por `products`) y crea el `StockLevel` inicial en 0 por cada bodega activa para productos con `track_inventory=True`; registrado en `bootstrap_core()` de `main.py` (patrón a replicar por Compras/Ventas/Restaurante cuando corresponda). UI: `InventoryView` con tabla de existencias (filas por debajo del mínimo resaltadas en rojo) + diálogo genérico `MovementDialog` (un solo diálogo parametrizado por `MovementMode` para las 4 operaciones, en vez de 4 diálogos casi idénticos). 20 pruebas de integración nuevas (74 en total); `ruff`/`mypy --strict` limpios; verificado visualmente de punta a punta incluyendo el flujo cruzado Productos→evento→Inventario a través del wiring real de `main.py` (no solo a nivel de servicio).
- **Refactor adicional de main.py**: `build_main_window` y las funciones `_build_*_content` ahora reciben el `Container` de DI en vez de que cada servicio nuevo agregue un parámetro más a la firma de `build_main_window`/`main()` — necesario porque ya son 7 servicios de aplicación y seguían creciendo. `_build_nav_panels(container, open_panel)` centraliza la lista de paneles disponibles; agregar un módulo con pantalla propia es agregar una línea ahí.
- Completado M6 con los módulos Clientes (`modules/customers`) y Proveedores (`modules/suppliers`): `CustomerManagementService` (CRUD + `register_credit_movement` con validación de cupo de crédito vía saldo derivado del último `CustomerCreditMovement.balance_after`, no una columna materializada + `register_loyalty_movement` con validación de saldo de puntos) y `SupplierManagementService` (CRUD simple). UI: `CustomersView` (tabla + cargos/abonos) y `SuppliersView`, combinadas en un panel "Clientes y Proveedores" gateado por el nuevo permiso `customers.manage` (agregado a `scripts/seed_demo_data.py`). 15 pruebas de integración nuevas (89 en total); `ruff`/`mypy --strict` limpios; verificado con smoke test end-to-end (login → panel → pestañas → creación real vía servicio).
- A partir de este módulo se redujo la verificación visual a un smoke test dirigido (login + navegación + una operación real) en vez de capturas de pantalla exhaustivas de cada estado, para mantener el ritmo en los módulos CRUD simples restantes — se retoma la verificación visual completa con capturas para módulos de mayor riesgo (Ventas, Caja, Licencias).
- Completado el módulo Caja (`modules/cash_register`), construido antes que Ventas porque `sales.cash_session_id` depende de él (dirección de dependencia corregida en MODULES.md: Caja no depende de Ventas, es al revés). `CashRegisterService`: apertura de turno (rechaza abrir dos turnos simultáneos en el mismo punto de caja), movimientos manuales de ingreso/egreso, cálculo de monto esperado (apertura + ventas/ingresos − egresos/devoluciones — el tipo `SALE` ya está contemplado en el cálculo aunque todavía nada lo publica), cierre con arqueo (`difference = contado - esperado`), eventos `CashSessionOpenedEvent`/`CashSessionClosedEvent`. UI: `CashRegisterView` (selector de caja, apertura/cierre, movimientos manuales, historial). 9 pruebas de integración nuevas (98 en total); `ruff`/`mypy --strict` limpios; verificado end-to-end (abrir turno → ingreso manual → cerrar con arqueo cuadrado). Se agregó "Caja Principal" al seed y el permiso `cash_register.manage` ya estaba seedeado.
- **Bug de datos de prueba (no de producción) encontrado por la FK ya corregida**: las pruebas de caja usaban `opened_by_user_id=1` inventado; con `PRAGMA foreign_keys=ON` ya activo, esto falla con `IntegrityError` real. Corregido agregando un fixture `user_id` que crea un usuario real — otra confirmación de que la corrección de FK de la Fase M4 sigue pagando dividendos en detectar datos de prueba inválidos.
- Completado el módulo Ventas (`modules/sales`), el más grande y crítico hasta ahora. **Decisión de arquitectura documentada en ARCHITECTURE.md §5b** (nueva sección): efectos que deben suceder atómicamente junto con la venta (descuento de stock, movimiento de caja, cargo a crédito) se implementan como llamadas directas síncronas a los servicios de aplicación de Inventario/Caja/Clientes — no como eventos — porque una falla en cualquiera de ellos debe abortar la venta completa; `SaleCompletedEvent`/`SaleVoidedEvent` sí se publican, pero solo para consumidores de solo lectura (Auditoría, Reportes, Notificaciones, Sincronización — ninguno implementado todavía). Limitación aceptada y documentada: cada servicio abre su propia transacción, así que no hay atomicidad distribuida real; se mitiga validando todo (stock suficiente, turno de caja abierto, cupo de crédito disponible) *antes* de mutar nada, en el orden de menos a más reversible.
  - `SalesService.complete_sale`: recibe ítems + pagos (posiblemente mixtos: efectivo/tarjeta/transferencia/crédito), calcula precios e impuestos leyendo `ProductRepository` (se le agregó `get_taxes_for_product` porque `get_tax_codes` solo devolvía nombres, no tasas), valida que el total de pagos coincida con el total de la venta, valida stock suficiente vía `InventoryService.get_available_quantity` (método nuevo), valida turno de caja abierto vía `CashRegisterService.require_open_session` (método nuevo — **bug real encontrado**: sin él, una venta 100% a crédito o tarjeta nunca validaba que la sesión de caja existiera o estuviera abierta), y valida cupo de crédito vía `CustomerManagementService.register_credit_movement`. Solo entonces persiste la venta y aplica los efectos.
  - `SalesService.preview_sale`: reutiliza el mismo cálculo de precios (`_price_items`, extraído como función pura) para que la UI del carrito muestre el total en tiempo real sin persistir nada — usa un DTO dedicado `SalePreviewDTO` (no `SaleDTO` con campos falsos como `id=0`, que habría sido un olor de código).
  - `SalesService.void_sale`: revierte stock (entrada), efectivo (`CashRegisterService.register_refund_movement`, método nuevo) y crédito (abono), y marca la venta `REFUNDED`.
  - UI: `SaleView` (carrito interactivo con vista previa en tiempo real, selección de cliente, pagos múltiples) y `SalesHistoryView` (listado + anulación), en un panel "Ventas" gateado por `sales.create`.
  - 16 pruebas de integración nuevas (114 en total); `ruff`/`mypy --strict` limpios; verificado visualmente de punta a punta con capturas reales (carrito → pago → venta completada → historial), incluyendo el descuento de stock correcto (20→18 unidades tras vender 2).
- **Nota para retomar sobre verificación con Qt**: los scripts de verificación manual con `QTest`/`QMessageBox` deben interceptar `QMessageBox.information`/`.warning` (no solo `QMessageBox.exec`, que no cubre las llamadas estáticas que usan `exec()` internamente en C++) o el script se cuelga indefinidamente esperando un clic que nunca llega.
- Avanzado M9 con el módulo Reportes (`modules/reports`): `ReportsService` con reportes de Ventas, Productos vendidos, Inventario actual y Caja (todos por rango de fechas donde aplica), leyendo directamente los modelos de `sales`/`cash_register`/`customers` (dependencia documentada en MODULES.md). Exportación genérica real a PDF (ReportLab, con estilos de tabla) y Excel (openpyxl) vía `ReportsService.export(...)` — un solo exportador reutilizable por cualquier tipo de reporte futuro (headers + rows de texto), no uno por reporte. UI: `ReportsView` con selector de tipo/rango y botones de exportación con `QFileDialog`. Se agregaron `types-openpyxl`/`types-reportlab` como dependencias de desarrollo para que `mypy --strict` los tipara correctamente. 7 pruebas de integración nuevas (121 en total) — incluyen verificación real de que el PDF generado empieza con la cabecera `%PDF` y que el Excel generado se puede reabrir con `openpyxl.load_workbook` y tiene los valores correctos, no solo "no lanzó excepción". `ruff`/`mypy --strict` limpios; verificado end-to-end (panel accesible, exportación genera archivos reales de tamaño > 0).
- Reportes pendientes (no implementados aún, dejados fuera del alcance de esta pasada): Ganancias/Pérdidas (requiere comparar `SaleItem.line_total` contra `Product.cost_price`, no implementado), Clientes (historial de compras/deudas), Usuarios (actividad/ventas por cajero), Impuestos (recaudo por tipo de impuesto), Estadísticas (dashboard agregado). Se pueden agregar como métodos nuevos de `ReportsService` reutilizando `export()` sin cambios — el exportador ya es genérico.
- Completado el módulo Licencias (`modules/licensing`), implementando el mecanismo diseñado en ARCHITECTURE.md §9 de punta a punta: tokens firmados Ed25519 (`license_key` = payload JSON en base64 + firma en base64), huella de hardware de un solo sentido (`get_hardware_fingerprint`), y **marca de agua monótona cifrada** (`WatermarkStore`, Fernet con clave derivada de la huella de hardware) que impide que retroceder el reloj del sistema burle una licencia por vencer — la verificación de expiración siempre usa `max(reloj_actual, última_marca_vista)`, nunca el reloj crudo. Vendedor vs. cliente separados de verdad: `scripts/generate_license.py` (herramienta del vendedor, usa la clave privada) vs. la app (solo conoce la clave pública embebida en `embedded_public_key.py`); la clave privada de desarrollo (`scripts/vendor_private_key.dev.txt`) está en `.gitignore` y nunca se commitea. UI: pantalla de activación que además sirve como **bloqueo elegante** al arrancar la app cuando no hay licencia válida (antes incluso del login, tal como pide el spec) — `build_main_window` verifica la licencia primero y solo muestra el login si el resultado es `VALID`.
  - **Bug real encontrado y corregido, con alcance mucho más amplio que Licencias**: `License.expires_at`/`issued_at` (y ~25 columnas de fecha/hora más en 14 módulos distintos) usaban `Mapped[datetime]` sin marcar el tipo de columna, y SQLite **nunca preserva `tzinfo`** en un viaje de ida y vuelta (verificado empíricamente: ni siquiera `DateTime(timezone=True)` lo arregla en SQLite, a diferencia de PostgreSQL). Comparar esas fechas "naive" leídas de la BD contra un `datetime.now(timezone.utc)` fresco lanzaba `TypeError`. Corregido con un `TypeDecorator` nuevo y reutilizable (`core/database/types.py::UTCDateTime`) que normaliza a UTC al guardar y reasigna `tzinfo=UTC` al leer, aplicado a los mixins compartidos (`TimestampMixin`) y a las ~25 columnas afectadas en `audit`, `auth`, `billing`, `backups`, `cash_register`, `customers`, `inventory`, `notifications`, `promotions`, `purchasing`, `restaurant`, `sales`, `sync`, `users`. No requirió una migración Alembic nueva (el tipo SQL subyacente no cambia, solo el marshaling Python). Este es exactamente el tipo de deuda técnica silenciosa que el spec pide detectar y corregir sin esperar a que rompa algo en producción.
  - **Bug visual real encontrado y corregido en verificación con capturas**: dos `QLabel` con `wordWrap=True` consecutivos en un `QVBoxLayout` se superponían porque Qt no calcula `heightForWidth` correctamente sin un ancho explícito — corregido fijando el ancho de los labels envueltos al ancho de contenido de la tarjeta. Además, el campo de huella de hardware se cambió de `QLabel` a `QLineEdit` de solo lectura porque un hash hex largo sin espacios no hace wrap y quedaba cortado — así además el usuario puede seleccionarlo y copiarlo, que es el punto (debe enviárselo al proveedor).
  - 9 pruebas de integración nuevas (130 en total), incluyendo una prueba dedicada al escenario central de retroceso de reloj. `ruff`/`mypy --strict` limpios; verificado visualmente de punta a punta (bloqueo sin licencia → activación real con clave generada por `scripts/generate_license.py` → acceso al login).
- **Siguiente paso al retomar**: módulo Backups (`modules/backups`, único pendiente de M10) — copias manuales/automáticas de la base de datos SQLite (archivo único, así que un backup simple es copiar el archivo `.db` con `shutil.copy` dentro de una transacción consistente, o usar el comando `VACUUM INTO` de SQLite que genera una copia consistente sin bloquear escrituras), restauración, exportación/importación, y programación con APScheduler (ya en TECHNOLOGIES.md, todavía sin usar en el proyecto — este es el primer módulo que la necesita). Después: Sincronización (M11, servidor FastAPI embebido) y luego el Instalador de Windows (M12), que son los últimos dos módulos antes de que el proyecto esté funcionalmente completo según PROJECT_SPEC.md (fuera de Promociones/Restaurante/Cocina/Facturación-PDF, que quedaron documentados como pendientes de M7/M9 en entradas anteriores de este log).

## Cómo retomar el trabajo tras un corte de contexto

1. Leer este archivo completo.
2. Revisar `git log --oneline` para ver el último commit real aplicado.
3. Revisar la lista de tareas activa (TaskList) para ver qué módulo está `in_progress`.
4. Continuar automáticamente desde el "Siguiente paso al retomar" sin pedir confirmación, salvo que se haya detectado una inconsistencia entre este archivo y el estado real del código (en ese caso, corregir este archivo primero para que refleje la realidad, y luego continuar).
